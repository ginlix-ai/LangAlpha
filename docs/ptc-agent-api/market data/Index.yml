info:
  name: Index
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/market-data/intraday/indexes/:symbol
  params:
    - name: symbol
      value: GSPC
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
