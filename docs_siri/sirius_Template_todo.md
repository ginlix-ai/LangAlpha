todo：
1、 金融领域要求可溯源，所有的核心分析需要带索引：自己的思考、计算逻辑、原始数据报告、网站索引，并且保留原始的代码、数据、报告保留到本地，保存在输出的json文件里
2、 要有独立的审核agent对于AI的分析过程进行审核，并且给出专家建议，可以强制让AI获取专家建议重新运行；
3、 数据预处理：把财报数据、电话会数据等所需要的数据全部准备好；
  财报：hk-financial-downloader（港股），download-financial-pdfs（A股）
  电话会：fmp（美股、港股），
4、 要记录一下AI分析大概会用多少token；


从研究报告再到结构化分析，不要一次性输出结构化内容；
比如第一章节的商业模式分析，应该输出一个markdown，然后再进行结构化输出

动态生成skills并且注册，然后监控各个公司的需要关注的点；

体验：
1、 skills：数据和知识管理：更新财报、研报、数据（csv），专门skills整理，并且写到数据知识库的目录，可以加载到AI agent里。知识管理员：增删查改和目录修改
2、 结构化分析：
  2.1 产业链分析 （单独skills）
  2.2 公司分析（单独skills）
3、 持久化分析：新增财报、研报、新闻等数据，加入到知识管理然后影响后续的结构化分析
每一个skills的分析都需要一个独立的技能，然后技能之间可以互相调用；

关于分析方法：
1、 用markdown的文章形式进行分析，然后要支持可结构化、量化；
2、 
